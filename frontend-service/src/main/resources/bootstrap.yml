spring:
  application:
    name: frontend-service
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
