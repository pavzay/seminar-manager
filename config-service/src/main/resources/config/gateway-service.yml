server:
  port: 8080

zuul:
  routes:
    auth-service:
      path: /uaa/**
      serviceId: auth-service
      sensitiveHeaders:
    seminar-service:
      path: /seminars/**
      serviceId: seminar-service
    speaker-service:
      path: /speakers/**
      serviceId: speaker-service
